
   function circleArea(r){
        console.log(`radius: 5 to be circle Area is ${Math.PI*r**2}`);
    }
    function circleCircumference(r){
       return console.log(`radius: 5 to be Circle circum ference is ${Math.PI*r*2} `)
    }
    

module.exports={
    circleArea,
    circleCircumference
}